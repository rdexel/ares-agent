# Copy to .env and fill in. .env is gitignored.

# Hunter.io — email finding/verification (scripts/hunter.js)
# Get a key at https://hunter.io → Account → API
HUNTER_API_KEY=

# Note: HUBSPOT_ACCESS_TOKEN and ANTHROPIC_API_KEY are exported in your shell
# profile (~/.zshrc), not stored here. See SETUP.md.
